name: second_brain
description: Minimal MVP app for AI Second Brain
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  dio: ^5.6.0
  flutter_secure_storage: ^8.0.0
  provider: ^6.1.2
  shared_preferences: ^2.3.2
  intl: ^0.19.0
  share_plus: ^10.0.2
  record: ^6.1.2
  permission_handler: ^11.3.1
  path_provider: ^2.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
